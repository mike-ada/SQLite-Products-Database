-- sqlite documentation: https://sqlite.org/docs.html

.header on --set the flag to display field heading
.mode column --set the flag to enable tabular output with one row per line
.timer on --set the flag to show the execution time

.open "products_sqlite.db" --open the products database

